# Task: T4-009 - Backend Service
# From: specs/phase4-kubernetes/spec.md §4.5

apiVersion: v1
kind: Service
metadata:
  name: todo-backend-svc
  namespace: todo-app
  labels:
    app: todo
    component: backend
spec:
  type: ClusterIP
  selector:
    app: todo
    component: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
