# Task: T4-011 - Frontend Service
# From: specs/phase4-kubernetes/spec.md §4.5

apiVersion: v1
kind: Service
metadata:
  name: todo-frontend-svc
  namespace: todo-app
  labels:
    app: todo
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: todo
    component: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
